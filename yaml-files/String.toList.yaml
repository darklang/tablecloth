module: String
name: toList
docstring: Returns a {!List} of the individual characters in the given string
parameters:
  - name: a
    type: string
returnType: list<char>
# For compatibility
constraints:
  - no-ocaml
  - no-rescript
tests:
  - inputs: "Standard"
    output: >-
      ['S', 't', 'a', 'n', 'd', 'a', 'r', 'd']
examples:
  - inputs: "Standard"
    output: >-
      ['S', 't', 'a', 'n', 'd', 'a', 'r', 'd']
added: 0.1.0
